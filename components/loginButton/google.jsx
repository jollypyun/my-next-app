const Google = () => {
    const googleClientId = process.env.NEXT_PUBLIC_GOOGLE_CLIENT_ID
    const goolgeRedirectUrl = process.env.NEXT_PUBLIC_GOOGLE_REDIRECT_URL
    const googleAuthUrl = `https://acoounts.google.com/o/oauth2/v2/auth?response_type=code&client_id=${googleClientId}/`

    return (
        <></>
    )
}